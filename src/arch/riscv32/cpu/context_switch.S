/*
 * context_switch.S - RISC-V context switch primitive.
 *
 * Responsibilities:
 * - save the current execution context
 * - restore the next execution context
 * - implement the architecture-specific register transfer needed for task switching
 *
 * (not implemented)
 */
