/*
 * trapframe.h - Saved register layout for trap handling.
 *
 * Responsibilities:
 * - define the memory layout used to save CPU state on trap entry
 * - match the assembly logic in trap_entry.S
 *
 * (not implemented)
 */
