/*
 * intc.c - ESP32-C6 interrupt backend.
 *
 * Responsibilities:
 * - configure and service SoC/platform interrupt handling state
 * - provide the platform side of the generic interrupt interface
 */
#include <kernel/irq.h>

void irq_init(void) {
}

void irq_enable_global(void) {
}

void irq_disable_global(void) {
}
